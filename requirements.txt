# ChatGPT Auto Register - Multi-user Platform
# Phase 1: Phone Registration + Phase 2: OAuth + SUB2API + Web GUI

# Core
curl_cffi>=0.12.0
requests>=2.28.0
PySocks>=1.7.0
urllib3>=2.0.0

# Web GUI
flask>=2.0.0
flask-cors>=4.0.0

# Multi-user server
psycopg2-binary>=2.9.0
pyjwt>=2.0.0
bcrypt>=4.0.0
apscheduler>=3.10.0

# iCloud (Chrome cookie decrypt, Windows only)
pycryptodome>=3.15
pywin32>=305; platform_system == "Windows"

# Plus 升级 (浏览器捕获 Midtrans URL)
playwright>=1.40.0

# Phase 2: OAuth + bind email
beautifulsoup4>=4.9.0
